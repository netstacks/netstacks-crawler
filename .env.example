# Optional overrides for `docker compose up`. Copy to `.env` to use — all have
# sensible defaults, so an empty/absent .env works out of the box.

# Host port the web UI is published on (default 5000)
# CRAWLER_PORT=5000

# Postgres password (default: crawler)
# POSTGRES_PASSWORD=crawler
